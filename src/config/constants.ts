let prod = true;
export const host = prod ? 'nickpyth-hakaton2024-b385.twc1.net' : 'localhost';
export const port = prod ? '/api' : ':8080/api';
export const secure = prod ? 'https' : 'http';
export const dateTimeFormat = "DD-MM-YYYY HH:mm";